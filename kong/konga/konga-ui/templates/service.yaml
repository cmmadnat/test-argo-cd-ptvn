apiVersion: v1
kind: Service
metadata:
  name: {{ include "konga-ui.fullname" . }}
  labels:
    {{- include "konga-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "konga-ui.selectorLabels" . | nindent 4 }}
