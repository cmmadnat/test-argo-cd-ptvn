apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "konga-ui.fullname" . }}-test-connection"
  labels:
    {{- include "konga-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "konga-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
